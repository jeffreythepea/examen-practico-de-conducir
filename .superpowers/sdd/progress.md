# Subagent-Driven Development Progress

Stage 1 Task 1: complete (uncommitted task diff, review clean after fixes; `npm test` 4/4).
Stage 1 Task 2: complete (uncommitted task diff; catalog tests passing at continuation baseline).
Stage 1 Task 3: complete (uncommitted task diff; localization tests passing at continuation baseline).
Stage 1 Task 4: complete (uncommitted task diff, review clean after fixes; focused tests 10/10).
Stage 1 Task 5: complete (uncommitted task diff, review clean after nested validation fixes; full tests 33/33).
Stage 1 Task 6: complete (uncommitted task diff, two-voice ElevenLabs corpus with 180 verified MP3s; review clean; full tests 50/50).
Stage 1 Task 7: complete (uncommitted daily-practice UI, replay focus lifecycle fixed; independent review clean; full tests 76/76; browser smoke clean).
Stage 1 Task 8: complete (uncommitted release audit and documentation; actual release-set credential scan; independent review clean; full tests 80/80; Piso extraction regression gates pass).

Stage 2 plan: `docs/superpowers/plans/2026-07-17-stage-2-action-surfaces.md`
Manual reference verified: `/Users/jeffreypease/Downloads/PZ49X-52A96-EN.pdf` is Toyota Europe part `PZ49X-52A96-EN`, Yaris Hybrid, model year 2019, line-off date 2019-05-01; internal footer `YARIS_HV_OM_Europe_OM52A96E`. It covers optional equipment, so confirm actual trim during lessons.
Vehicle transmission uncertainty (superseded implementation note): Jeffrey requested a manual-transmission test vehicle so his licence will not be restricted, but the verified 2019 Yaris Hybrid reference is automatic e-CVT (manual pp264-265). The earlier provisional selector-P interaction has now been replaced by a generic manual procedure grounded in RGC Article 92; the stable external surface ID remains for progress compatibility. School confirmation of the actual test vehicle, controls, transmission, and expected procedure is still required.
Stage 2 Task 1: complete (uncommitted seeded immutable surface contract; review clean after deep-freeze/JSON-safety/unique-target fixes; full tests 88/88).
Stage 2 Task 2: complete (uncommitted normalized geometry utilities; review clean; full tests 93/93).
Stage 2 Task 3: complete (uncommitted pure junction/roundabout v2 surfaces; activation deferred atomically to Task 7; review clean after sequencing/reveal/regression fixes; full tests 102/102).
Stage 2 Task 4: complete (uncommitted action-matched manoeuvre templates; review clean after U-turn/reveal/sign/accessibility fixes; full tests 115/115).
Stage 2 Task 5: complete (uncommitted wheel/Yaris securing controls grounded in manual pp236/264/269; review clean after reduced-motion polish; full tests 126/126).
Stage 2 Task 6: complete (uncommitted original Yaris schematics; manual topology/accessibility/oracle issues fixed; five 1024x768 screenshots clean with 62.875px minimum targets, no overflow, and clean console; independent re-review approved; full tests 142/142). Recovery details: `.superpowers/sdd/stage2-task-6-report.md`.
Stage 2 Task 7: complete (uncommitted atomic v2 integration; reducer-owned fail-closed response semantics; independent final re-review approved; full tests 157/157; complete 30-command real-app matrix at 1024x768 plus setup/results at 1366x768; console clean). Browser limitation: export download event and confirm/file-picker import could not be completed through automation; automated backup tests remain green and manual review smoke is requested. Recovery report: `.superpowers/sdd/stage2-task-7-report.md`.
Stage 2 Task 8: complete (uncommitted release audit/documentation; corrected review finding to distinguish no manual-transmission-specific training from provisional automatic-hybrid `c-inmov` securing sequence; focused release audit 5/5 and full suite 158/158 after the correction; clean whitespace check; all four Piso Asturiano extraction gates pass). Jeffrey review and manual export/import smoke remain required before any commit or publication. Recovery details: `.superpowers/sdd/stage2-task-8-report.md`.
Stage 2 final review: complete and APPROVED after fixes for insecure-LAN UUID fallback, hardened LAN serving/documentation audit, seeded semantic shuffling, and accessible provisional `c-inmov` disclosure. Fresh controller gates: repository tests 167/167, `git diff --check` clean, all four Piso gates pass, zero staged files; post-fix 1024x768 browser check clean with no console errors. Remaining Jeffrey actions: manual export/import smoke, gameplay/diff review, eventual school confirmation of actual manual test-car procedure, then Jeffrey-only commit/push if satisfied. Recovery details: `.superpowers/sdd/stage2-final-review.md`.

Photo-backed clarity build, 2026-07-19: Tasks 1–6 complete. Checkpoints `6a66538`, `3f8348b`, and `f31cbe6` are pushed to `origin/main`. Seven illustrative photo scenes replace active abstract precheck drawings while retaining stable IDs; lighting and door targets use unobstructed native symbols, and engine targets use precise component icons. Overtaking now separates learner and lead vehicles. `c-inmov` now uses the Article 92 generic manual state. The final 1024×768 visual matrix and release ledger are in `.superpowers/sdd/precheck-photo-final-review.md`. Final gates: 174/174 repository tests, 7/7 release-audit tests, clean `git diff --check`, bilingual AI-voice disclosure visible, audio replay clean, and zero browser console errors. Final checkpoint commit/push remains.

Native photo controls cleanup, 2026-07-19: ready for Jeffrey review and uncommitted. Six recognizable cabin/exterior photo scenes now use ring-only targets; engine-bay component icons remain; high-beam placement and bonnet/tailgate reveal-label separation were browser-audited at 1024×768. Hint-policy labels are explicit in both languages and on-demand text resets per question. Fresh gates: 176/176 repository tests, 7/7 release audits, clean whitespace, and zero browser log messages. Recovery details: `.superpowers/sdd/native-photo-controls-final-review.md`.

Photo-like driving mockups, 2026-07-19: three review-only plates and a bilingual static overlay page are complete and uncommitted under `docs/mockups/driving-surfaces/`. Overtaking has distinct learner/lead vehicles plus collision-free pass and safe-follow space; the roundabout uses a bottom entry plus four code-native exits and can later pair with the existing variable exit-count model; legal stopping uses a clear curb, driveway, and pedestrian crossing. Focused asset/isolation/provenance tests pass 6/6. The in-app automation client blocked both loopback hostnames, so Jeffrey's visual review uses the existing local server URL. No production source references the mockup directory. A separate requested correct-ding/incorrect-buzzer feature is captured for design approval and has not been implemented.

Feedback sound cues checkpoint 1, 2026-07-19: synthesized Web Audio engine complete and uncommitted in `src/feedback-audio.js`. Exact cue vocabulary is correct/incorrect/spanish-hint; all schedules end before 600 ms; disabled, busy, unsupported, construction-failure, and resume-failure paths fail closed. Focused tests pass 4/4. Settings and application triggers remain pending.

Feedback sound cues checkpoint 2, 2026-07-19: persisted bilingual `Feedback sounds` / `Sonidos de respuesta` setup control complete and uncommitted. The setting defaults on, round-trips false, migrates old backups to on, and rejects invalid values. Focused storage/i18n/app-smoke tests pass 32/32. Application triggers and final listening review remain pending.

Feedback sound cues checkpoint 3, 2026-07-19: application trigger wiring complete and uncommitted. Accepted unaided/assisted answers request the correct cue, answered misses request the incorrect cue, the first accepted on-demand Spanish reveal requests the hint cue, and timeouts/rejections/repeats/non-feedback transitions stay silent. Command and replay audio stop any active cue; cue failures remain outside scoring. Focused state/smoke/audio tests pass 45/45. Full release verification and Jeffrey's listening review remain pending.

Feedback sound cues final build, 2026-07-19: ready for Jeffrey review and uncommitted. Synthesized correct, incorrect, and Spanish-hint cues are integrated with a persisted bilingual on/off setting. Fresh gates: 192/192 repository tests, 7/7 release audits, and clean `git diff --check`. At 1024×768 the setup control was browser-verified to persist both Off and restored On; the bilingual AI-voice disclosure remained visible and browser warnings/errors were empty. Automated browser control cannot judge speaker volume or timbre, so Jeffrey's normal-volume listening review remains required; verify one hint plink, one correct chime, one answered-miss buzzer, silent timeout, and mute suppression.

Setup copy clarity checkpoint, 2026-07-19: ready for Jeffrey review and uncommitted. `Weak and due first` is now `Previously missed questions` / `Preguntas falladas anteriormente`; the unavailable hint policy now explicitly says `No Spanish text or Show Spanish button` / `Sin texto en español ni botón Mostrar español`. Stable values and behavior are unchanged. The exact-copy test failed twice against the old strings, then passed 12/12; full suite passes 193/193 and `git diff --check` is clean. Both locales were verified in the live setup screen with no browser warnings/errors, and the interface was restored to English.

Gameplay feedback production checkpoint 1, 2026-07-19: manual immobilization dead-end fixed and uncommitted. A fully configured wrong slope gear now enters an incorrect reveal with Continue; partial sequences remain unscored. The slope marker is a directional side-view car SVG with body, windows, wheels, and front marker. Focused control/app-state tests pass 46/46. Photo-road activation and session-length changes remain pending.

Gameplay feedback production checkpoint 2, 2026-07-19: production driving-photo registry and four local raster plates complete and uncommitted. Three approved mockups were promoted without alteration; a matching five-exit roundabout plate was generated with the built-in image workflow and inspected for exactly one bottom entry plus five outgoing mouths. Registry IDs, bilingual alt text, frozen AI-generated provenance, local paths, and PNG integrity pass 2/2 focused tests. Renderer activation remains pending.

Gameplay feedback production checkpoint 3, 2026-07-19: approved photo plates are activated and uncommitted for overtaking, parking, stopping, and both four/five-exit roundabouts. Raster scenery sits below existing code-native targets, routes, signs, reveal markers, and bilingual accessibility text; U-turn and junction remain code-drawn by scope. Five-exit angles were realigned to the photographed lower-right/right/top/left/lower-left mouths while preserving non-overlap and seeded distribution. Focused registry/manoeuvre/spatial suites pass 29/29. Browser geometry review and session lengths remain pending.

Gameplay feedback production checkpoint 4, 2026-07-19: session choices are now concrete and uncommitted: 5, 10, or 15 commands (`5/10/15 preguntas`), with fresh-state default 10. Existing `short`/`medium`/`all` values remain backup-compatible and now map to 5/10/15; pools smaller than the selected limit cap naturally. Focused training/storage/i18n tests pass 37/37. Integrated full/browser verification remains pending.

Gameplay feedback production final build, 2026-07-19: ready for Jeffrey review and uncommitted. The manual immobilization wrong-gear dead end now reaches an incorrect reveal with Continue; the slope marker is a directional car. Overtaking, parking, voluntary stopping, and four/five-exit roundabouts use packaged photo base plates beneath unchanged code-native targets and routes; U-turn and junction remain code-drawn by approved scope. Session length is selectable as 5/10/15 with fresh default 10. Fresh gates: 199/199 repository tests, `npm run release:check` green, and clean `git diff --check`. Live browser verification confirmed the urban-road photo loaded at 1536×1024 into an exact 3:2 stage, all three response targets measured 68×61 CSS pixels, and browser warnings/errors were empty. Both locales exposed 5/10/15 copy, clarified practice-order and unavailable-hint labels, and the bilingual AI-voice disclosure. The setup was restored to English and Jeffrey's prior Prechecks / 0.75× / 5 questions / Previously missed configuration after the smoke. No commit or push was performed.

Overtaking target-alignment fix, 2026-07-19: ready for Jeffrey review and uncommitted. Root cause was reuse of parallel-lane coordinates on the new perspective road photo. Both seeded templates now place the passing choice on visible opposing-lane asphalt and the follow choice on open right-lane asphalt between the photographed cars; photo geometry metadata and reveal-route entry points match the embedded vehicles. The focused suite passes 14/14 across its 100-seed non-overlap sweep. A live 3:2 render measured the choices at approximately `(43.5%, 39.5%)` and `(59.7%, 54.9%)`, each at least 48×44 CSS pixels, with neither choice on grass or the learner car. Full verification follows this checkpoint.

Photo target alignment and defaults final build, 2026-07-19: ready for Jeffrey review and uncommitted. Fresh saves now default to Mixed practice and 10 commands; existing saved selections and backups remain unchanged. All two overtaking, two parking, two stopping, four-exit roundabout, and five-exit roundabout variants use photo-specific bounded anchors. Crosswalk and driveway options sit on the visible photograph without redundant SVG drawings; synthetic restriction markings and no-parking/no-stopping signs remain only where the photo lacks that information. Automated seed sweeps verify manoeuvre anchors across 64 seeds per family, roundabout mouths across 64 seeds per exit count, non-overlap across 100 seeds per surface, and minimum touch geometry. The eight-card live browser matrix measured every target at 63×56 CSS pixels, showed all choices on their intended physical feature or exit mouth, and produced zero warnings/errors. Fresh gates: 202/202 repository tests, `npm run release:check` green, and clean `git diff --check`. No commit or push was performed.

Voluntary-stop right-curb correction, 2026-07-19: ready for Jeffrey review and uncommitted. Jeffrey's reveal screenshot showed the accepted target in the left traffic lane. The stable legacy target ID is preserved, but its bounded anchor now sits on the clear lower-right curb segment at approximately `(67%, 59%)`; the no-stopping choice remains farther ahead at `(70.5%, 37.3%)` and the crossing choice remains on the photographed crossing. The regression test failed against the left-lane coordinate and now passes across the 64-seed photo-anchor sweep; focused manoeuvre tests pass 16/16. Live reveal verification measured all three controls at least 48×44 CSS pixels with zero browser warnings/errors. Full gates follow this checkpoint.

Photo-overlay and interaction recovery final build, 2026-07-19: ready for Jeffrey review and uncommitted. Photo SVG coordinates now map to the full 3:2 stage, so reveal routes, restriction markings, and prohibition signs share exact percentage coordinates with their HTML targets; sign geometry compensates for horizontal stretch and roundabout routes terminate inside their exit targets. Overtaking routes now begin ahead of the learner, pass through the chosen opposing-lane target, clear the lead vehicle, and return right. U-turn now uses a new AI-generated 1536×1024 photographic road/junction scene with stable `u-turn-photo-v1` provenance and bilingual alt text instead of the old synthetic road. Manual immobilization controls remain independently reversible, gear selection toggles off, and a bilingual Check answer control is disabled until engine, parking brake, and gear are all selected; scoring occurs only on confirmation. Live review confirmed toggle behavior, completion gating, corrected photo alignment, and zero browser warnings/errors. Fresh gates: 204/204 repository tests, `npm run release:check` green, and clean `git diff --check`. No commit or push was performed.

Parking/stopping position clarity refinement, 2026-07-19: ready for Jeffrey review and uncommitted. The accepted parking and voluntary-stop anchors now sit in the open lower-right curbside space rather than the travel lane, and all stopping choices display a narrow vertical car-position outline inside their unchanged iPad-sized tap areas. Stable target/result/feature IDs remain intact, while automated photo-anchor and reveal-state coverage protects both seeded parking variants and both seeded stopping variants. Focused manoeuvre tests pass 17/17; the full repository suite passes 205/205; `npm run release:check` and `git diff --check` are green. No commit or push was performed.

Dedicated parking and four-way junction photo build, 2026-07-19: ready for Jeffrey review and uncommitted. Parking now uses AI-generated `parallel-parking-gap-photo-v1`, with its accepted target centered in the unobstructed gap between two right-curb parked cars; the driveway, crossing, marked restriction, and no-parking alternatives remain physically or synthetically distinct, while voluntary stopping retains `urban-roadside-photo-v1`. Junction commands now use AI-generated `four-way-intersection-photo-v1` with stable left and right targets plus a `straight` / `continue-forward` distractor, so the task tests both the turn instruction and direction. Both 1536×1024 assets were generated with the built-in image workflow and inspected at original resolution. Sixty-four-seed sweeps protect parking and junction anchor placement, non-overlap, and touch size. Live 1024×768 review confirmed all targets sit on their intended parking gap, driveway, crossing, or outgoing asphalt; reveal routes terminate correctly; browser warnings/errors are empty. The full repository suite passes 206/206; `npm run release:check` and `git diff --check` are green. No commit or push was performed.

Command and phrasing expansion checkpoint 1, 2026-07-20: source audit and catalog layer complete. The visually reviewed Fermín PDF is primary; its Spanish brake-fluid text is authoritative, while washer fluid is a separate command supported by the diagram key and Toyota manual. The catalog now declares 36 commands (18 driving, 18 prechecks), adding straight ahead, indicator, brake fluid, washer fluid, position lights, and dipped headlights. Eighteen existing commands each gain one source-labeled supplementary phrasing, for 54 total phrasings. Catalog tests passed 9/9 after failing against the 30-command baseline. Runtime surfaces and audio remain intentionally pending in later checkpoints.

Command and phrasing expansion checkpoint 2, 2026-07-20: runtime behavior and all six response surfaces are implemented. Audio selection now samples every audio-backed phrasing for a command/speed and retains the chosen phrasing through hint, replay, reveal, and attempt logging. Straight ahead uses the existing photographed four-way junction. Brake and washer fluid use separate precise engine-bay targets; position and dipped headlights use isolated native-symbol targets; the indicator correction uses the photographed stalk arrows. All new UI labels are bilingual. Focused TDD suites pass and the full repository suite passes 213/213. Audio generation and final browser review remain pending.

Command and phrasing expansion checkpoint 3, 2026-07-20: the audio generator now plans every catalog phrasing, verifies and reuses valid production clips, checkpoints each new MP3 plus its manifest record to an ignored recovery directory, resumes from that recovery ledger, and leaves the published corpus untouched unless the complete staged corpus validates. The target corpus is 324 clips (54 phrasings × 2 voices × 3 speeds). The first ElevenLabs run reused all 180 published clips and safely recovered 136 of the 144 missing clips before the provider returned HTTP 401. Eight clips remain: the Sarah variants at 0.9× and 1× for `c-pre-posicion`, plus all six `c-pre-cruce` voice/speed variants. Recovery state is `.superpowers/sdd/audio-expansion-recovery/audio-expansion/`; rerun the same approved production generator command after quota access returns. Focused generator/audio tests pass 16/16, the full repository suite passes 216/216, and `git diff --check` is clean. The current local app is intentionally fail-closed because the published manifest remains the prior 180-clip corpus until the final eight clips can be generated and all 324 can be atomically published.

Browser Spanish speech fallback final build, 2026-07-20: complete and ready for Jeffrey review. The app still prefers packaged MP3 audio, but now automatically uses the browser's Spanish speech synthesis when a selected phrasing has no published clip or when its MP3 cannot be played. A completed fallback utterance is scored exactly like recorded audio; unsupported, interrupted, hidden-page, or failed speech remains unscored. The selected Spanish phrasing and speed remain stable through Replay and Show Spanish, and attempt history records the audio provider without breaking older backups. No runtime API key, credential, network TTS request, or browser-delivered secret was added. Live 1024×768 verification reached the unpublished `Encienda las luces de cruce` command, completed initial browser speech and Replay, revealed the same phrasing, accepted the correct dipped-headlight response, and produced no browser warnings/errors. The production corpus remains 180 clips, with 136 safely recovered clips and eight ElevenLabs clips still pending in `.superpowers/sdd/audio-expansion-recovery/audio-expansion/`. Manual iPad speech/replay smoke remains recommended.

Release B recovery checkpoint, 2026-07-22: implementation is uncommitted and intentionally mid-integration. Approved plan: `docs/superpowers/plans/2026-07-21-release-b-readiness-targeted-practice.md`.

- Domain foundation reviewed: `src/readiness.js`, `src/practice-selection.js`, `src/variant-coverage.js`, and `src/lesson-flags.js` plus their tests. Hermes Run 2's original readiness-view completion claim was inaccurate; the view and tests were replaced/repaired locally.
- Readiness view complete: pure escaped bilingual renderer in `src/readiness-view.js`, Release B copy in `src/i18n.js`, and scoped responsive styles in `styles.css`. Focused readiness/i18n suite passed 28/28; the subsequent complete suite passed 426/426.
- Schema/session boundary complete: storage schema is 3; fresh state uses `mode: 'recommended'` and `lessonFlags: []`; schema 1 migrates through 2 to 3; legacy `weakest-first` becomes `recommended`; validated lesson flags persist; active-session target is optional for version-1 compatibility and supports all Release B target kinds. The complete suite passed 429/429 at this checkpoint.
- Scheduler/playback domain integration complete: `createSession` delegates to `selectPracticeCommands`, supports `recommended`/`free` plus explicit targets, and the obsolete competing `nextWeakestFirst` implementation was removed. `SESSION_LENGTHS` is owned by `src/practice-selection.js` and re-exported by `src/training.js` to avoid a circular dependency. `selectPlaybackVariant` now accepts `(manifest, command, speed, fallbackSupported, attempts = [], rng = Math.random)` and uses `selectCoverageAwareVariant` for recorded candidates. Focused training/practice-selection/variant/app-state tests passed 100/100.
- RED evidence was observed before each integration: scheduler tests failed with `Unknown session mode: recommended`; playback tests failed because the old fifth argument was still treated as RNG. Both are now green.
- Controller/UI integration is NOT complete. `src/app.js` setup still renders the legacy `weakest-first` option, session creation does not yet pass `target`, `lessonFlags`, `now`, or attempt history into every new selection/playback call, the readiness renderer is not yet reachable from navigation, and lesson-flag save/resolve/reopen actions are not bound. Do not present this worktree as a review build yet.
- Important recovery caution: the last full-suite proof is 429/429 from before the latest scheduler/playback edits. The latest proof is the 100/100 focused command above. Run the full suite only after controller wiring or when diagnosing cross-suite regressions.
- Exact next sequence: (1) add failing app/controller smoke/state tests for readiness navigation, recommended/free setup options, target start, and flag lifecycle; (2) wire `renderReadinessView` and readiness data into `render()`/bind cycle; (3) pass targets and lesson flags into `startSession`, persist the target in `createActiveSession`, and pass attempts to playback selection; (4) bind create/update/resolve/reopen flag operations; (5) update results/setup entry points and bilingual labels as required; (6) run focused tests, `npm test`, `npm run release:check`, and `git diff --check`; (7) browser-check setup, readiness, one targeted session, resume, and both locales.
- Current modified/untracked file list is recoverable with `git status --short`; preserve all files because Run 1, repaired Run 2, schema, and scheduler work share this checkout. No commit or push has been made for Release B.

Release B controller checkpoint, 2026-07-22: Task 6 controller/UI wiring is implemented and uncommitted. Setup and results link to the readiness view; setup offers Recommended practice or Free practice; readiness phase/state filters, targeted practice, single-command practice, and lesson-flag create/edit/resolve/reopen actions are bound; active sessions persist their target; selection and recorded playback consume attempt history; and the readiness heading participates in managed screen focus. The focused controller/state/storage/training/i18n/readiness suite passes 126/126. The two failures recovered after interruption were stale source-regex assertions (old four-argument playback selection and quote-style-sensitive action matching), not runtime failures. Full-suite, documentation/release-audit, and browser/iPad acceptance remain pending. No commit or push has been made for Release B.

Release B final implementation checkpoint, 2026-07-22: Tasks 1–7 and automated browser acceptance are complete and uncommitted. Reveal now opens the same persisted lesson-note editor used by Readiness. Documentation records evidence rules, targeted selection, coverage semantics, local backup behavior, schema 3, and the deliberate absence of composite/engagement scores; release counts are derived from the actual 36-command, 76-phrasing catalog and 456-entry audio manifest. Fresh gates: 434/434 repository tests, 16/16 release audits, `npm run release:check` PASS, deterministic runtime package `fc312da117e91aeea567caaa4e194a1662f871d46c407727187930ed9428de4c` with 505 assets / 20,269,661 bytes / complete corpus, and clean 1024×768 plus 768×1024 browser checks with no horizontal overflow or browser warnings/errors. Create/save, targeted one-command practice, reveal editor, results entry, resolve/reopen, and both locales were exercised. Jeffrey's brief physical-iPad Release B smoke remains before publication. No commit or push has been made.

Release B independent-review hardening checkpoint, 2026-07-22: the targeted-practice phase is now selection-only and no longer rewrites saved setup preferences. Empty focused-practice pools are disabled and retain a bilingual controller fallback; completed sessions clear their persisted resume record; readiness focus identities include stable command/flag IDs with lifecycle fallbacks; lesson notes can be filtered by open/resolved status; unsaved editor drafts survive locale changes; and the browser no longer imposes a UTF-16 `maxlength` that conflicts with the 280-code-point domain rule. A new executable controller suite covers completion cleanup, live drafts, and repeated-action focus. Fresh gates: 442/442 repository tests, `npm run release:check` PASS, `git diff --check` clean, and deterministic runtime package `26464113ddc8f19c34e05fcddb9497045ddaf8b33f347a63187a833cdafea25a` with 505 assets / 20,274,007 bytes / complete corpus. Fresh browser smoke verified disabled target groups, open-note filtering, last-row focus restoration, EN/ES draft preservation, and no horizontal overflow. No commit or push has been made.

Release B physical-iPad acceptance, 2026-07-22: complete. Commit `b3241c2` was pushed to `origin/main`, the GitHub Pages build/deploy workflow completed successfully, and Jeffrey applied the staged offline update on iPad. He confirmed that the Readiness screen was accessible, targeted practice worked, a lesson note could be added from reveal, and the note could be resolved and reopened from Readiness. Release B is accepted; no physical-device review remains.

AI-assisted coding spikes planning checkpoint, 2026-08-04: approved design `docs/superpowers/specs/2026-08-04-ai-assisted-coding-spikes-design.md` and implementation plans `docs/superpowers/plans/2026-08-04-ai-assisted-coding-spikes.md`, `docs/superpowers/plans/2026-08-04-moving-road-spike.md`, and `docs/superpowers/plans/2026-08-04-swiftui-spike.md` are committed at `cd012cd`. The authoritative recovery ledger is `docs/experiments/2026-08-04-ai-assisted-spikes-lab.md`; the exact next task is Moving-road plan Task 1. Full Xcode.app is absent, so the later SwiftUI simulator spike requires the documented toolchain preflight and likely one manual installation. Neither spike has started.

Moving-road spike implementation checkpoint, 2026-08-04: the isolated browser experiment under `experiments/moving-road-spike/` is functionally complete and awaiting final gates. It uses the real `c-izq`, `c-recto`, and `c-der` catalog phrasings plus packaged Roger 0.9× audio, an immutable phase reducer, a fixed learner vehicle with a moving junction, Start-gated audio, delayed answers, replay, pause/resume, bilingual copy, exact reveal text, and reduced-motion handling. TDD evidence progressed from missing modules to 14/14 focused tests. Safari manual review covered both correct and incorrect outcomes, all three trials, audio, replay, pause, Try another, and locale switching; evidence is under `docs/experiments/evidence/2026-08-04-moving-road/`. In-app browser local-network blocking and the server's explicit-index requirement are recorded in the lab ledger. Current base is `137aafc`; exact next task is final verification, commit, and push. The later Swift preflight has now discovered Xcode is installed but its license remains unaccepted.

SwiftUI spike implementation checkpoint, 2026-08-04: the native simulator proof under `experiments/swiftui-spike/` is functionally complete and awaiting final gates. Xcode 26.6, the iOS 26.5 Simulator runtime, and XcodeGen 2.46.0 are available. The reproducible project bundles the checksum-verified production catalog and one Roger 0.9× recording, decodes all 36 commands, displays exact `c-recto` text through a bilingual SwiftUI surface, and plays the packaged Spanish audio. Focused Node suites pass 7/7 and the fresh simulator XCTest run passes 4/4. Manual iPad Pro 13-inch simulator review passed in portrait and landscape, including EN/ES switching and audio without an error state; evidence is under `docs/experiments/evidence/2026-08-04-swiftui/`. A Codex system-update restart was recovered from the tracked plan/lab and shared checkout. Current base is `8bb2523`; exact next task is final repository verification, commit, and push, followed by the written two-spike comparison.

AI-assisted coding spikes comparison checkpoint, 2026-08-04: both bounded proofs are committed through SwiftUI checkpoint `540cf0e`, and the descriptive comparison is complete in `docs/experiments/2026-08-04-ai-assisted-spikes-lab.md`. The moving-road browser proof delivered the richer interaction in 37 minutes with minimal tooling; the SwiftUI proof delivered a real native catalog/audio app in approximately 61 minutes but required Xcode, an 8.52 GB simulator runtime, XcodeGen, generated-project diagnostics, and Simulator permissions. Recommendation: retain both as test fixtures, keep production development web-first, and use the SwiftUI project for a later bounded comparison of repo-first Codex work versus direct agentic coding inside Xcode. Final gates are fresh: moving-road 14/14, repository 443/443, deterministic runtime package `26464113ddc8f19c34e05fcddb9497045ddaf8b33f347a63187a833cdafea25a` with 505 assets / 20,274,007 bytes / complete corpus, zero experiment assets in the package, bilingual production AI-voice disclosure intact, and clean `git diff --check`. Exact next task is comparison commit and push.

Moving junction production checkpoint 1, 2026-08-04: approved design and implementation plan are uncommitted at `docs/superpowers/specs/2026-08-04-moving-junction-road-design.md` and `docs/superpowers/plans/2026-08-04-moving-junction-road.md`. The persisted bilingual Road movement setting is complete: fresh and legacy settings default On, explicit Off round-trips, invalid values fail validation, version-1 resumable sessions normalize safely, and setup/restored-session plumbing includes the setting. Test-first RED failures were observed for missing defaults, validation, copy, and setup wiring; the focused storage/active-session/i18n/app-smoke gate now passes 70/70 and `git diff --check` is clean. Current base is `main`; no files are staged. Exact next task is Task 2, the one-shot audio-playback start lifecycle.

Moving junction production checkpoint 2, 2026-08-04: the audio player now accepts an optional one-shot initial-playback lifecycle observer. Recorded playback notifies only after `HTMLMediaElement.play()` resolves; browser-speech fallback notifies immediately before its play request; replay never repeats the initial observer; observer exceptions cannot alter scoring; and playback that cannot begin does not notify. RED evidence caught both missing recorded and fallback notifications. `tests/audio.test.js` now passes 16/16 and `git diff --check` is clean. Exact next task is Task 3, the pure immutable junction-motion controller and eased progress view.

Moving junction production checkpoint 3, 2026-08-04: the pure immutable junction-motion controller is complete and uncommitted in `src/junction-motion.js`. It models locked approach, interactive approach, waiting, answered, and failed transitions; derives elapsed/remaining time; and solves the standard CSS ease-in-out curve so reducer-frozen scale matches the rendered animation. Missing-module RED evidence was observed; `tests/junction-motion.test.js` passes 6/6. Exact next task is Task 4, the transformable photographed-junction scene.

Moving junction production checkpoint 4, 2026-08-04: the photographed `junction-v2` renderer now keeps its image, route overlay, and targets in one bounded transform layer while leaving reveal labels outside. The layer resumes one global six-second ease-in-out camera push via elapsed-time negative delay; reduced-motion CSS disables both animation and transform. Static junctions, roundabouts, manoeuvres, and prechecks retain their existing structure. Renderer/CSS RED evidence was observed; focused spatial/surface tests now pass 25/25 and `git diff --check` is clean. Exact next task is Task 5, reducer-owned initial-audio locking and motion lifecycle integration.

Moving junction production checkpoint 5, 2026-08-04: reducer-owned initial-audio and motion lifecycle integration is complete and uncommitted. Eligible junction audio start creates one retained surface and a locked moving prompt; successful playback completion unlocks the same surface and starts response timing; approach completion waits without scoring; answers and timeouts freeze the scene; replay preserves motion; and failed/interrupted initial playback returns to an unscored static retry. Static, reduced-motion, non-junction, and surface-generation-failure paths retain the existing loading behavior. RED evidence covered the previously unrecognized start and missing motion fields; focused app-state/motion tests pass 47/47 and `git diff --check` is clean. Exact next task is Task 6, browser lifecycle, render, timer, and animation-end coordination.

Moving junction production checkpoint 6, 2026-08-04: browser lifecycle coordination is complete and uncommitted. Actual recorded or fallback speech start dispatches the eligible junction into its locked moving prompt; playback completion unlocks it, render-only motion views resume the original timeline, CSS animation completion freezes without scoring, and the optional response timer cannot begin during initial audio. Road movement Off, reduced-motion media preference, non-junction commands, and callback/generation failures use the existing static loading path. Prompt and reveal share the same motion transform; the latter freezes at answer time. Controller RED evidence preceded implementation; the nine-suite focused integration gate passes 159/159 and `git diff --check` is clean. Exact next task is Task 7, release documentation, full runtime verification, and browser/manual review.

Moving junction production checkpoint 7, 2026-08-04: the implementation is complete and uncommitted pending Jeffrey's review. Documentation and runtime packaging cover the persisted bilingual Road movement setting and the junction-only six-second camera push. Live browser acceptance found and repaired one reveal-layout defect: the motion viewport now clips only the transformed image/overlay/targets, while the localized Correct road label remains visible outside it. At an iPad-landscape 1180×820 viewport, the moving junction reached its waiting state without horizontal or vertical overflow, replay preserved the frozen final transform, the answer reveal showed its route and result label, Road movement Off persisted across reload, Spanish setup copy rendered correctly, and roundabouts remained static. Browser warnings/errors were empty. Fresh gates: 465/465 repository tests, `npm run release:check` PASS, deterministic runtime package `f56fff11af98b432b66b2929425fe8cb935c0b2076bc59a240cbf5e8380e5bb9` with 506 assets / 20,286,376 bytes / complete corpus, and clean `git diff --check`. No files are staged and no commit or push was performed.

Moving junction physical-iPad acceptance, 2026-08-05: Jeffrey opened the verified uncommitted runtime package over the Mac's LAN server on a physical iPad and confirmed that it works. The moving-junction build has passed its required device review. Changes remain uncommitted and unstaged for Jeffrey's final source review and commit.

Expanded road-motion design checkpoint 1, 2026-08-05: brainstorming is in progress and no production code has changed. Jeffrey approved a temporary scope boundary of all photo-backed road exercises—roundabouts, U-turns, overtaking, parking, and voluntary stopping—while leaving prechecks, vehicle controls, manual immobilization, and text-option questions static. In the visual companion he selected Approach B, a shared motion lifecycle with family-specific scale and focal-point calibration, over a uniform camera push or full post-selection manoeuvre animation. The current recommendation awaiting confirmation is to preserve the accepted junction timing: Spanish audio and motion start together; choices unlock as soon as initial audio ends; motion may continue to the six-second endpoint; unanswered exercises then wait without scoring. Exact next task: confirm that timing choice, propose the bounded architecture and family calibration rules, and obtain section-by-section design approval before writing `docs/superpowers/specs/2026-08-05-expanded-road-motion-design.md`. The visual-companion session is `/Users/jeffreypease/Projects/examen-practico-de-conducir/.superpowers/brainstorm/55353-1785919277/`; its URL key is local-only and must not be committed.

Expanded road-motion design checkpoint 2, 2026-08-05: Jeffrey approved the current junction timing for all moving road exercises: motion and Spanish audio start together; choices unlock when audio ends; motion may continue to the shared six-second endpoint; unanswered scenes then wait without scoring. The implementation remains design-only with no production code changed. The proposed remaining architecture is to generalize `junction-motion.js` into a road-motion domain module, add a declarative calibration registry keyed by surface family and scene variant, wrap each approved photo renderer's image/overlay/targets in the same bounded transform viewport, and keep the existing Road movement preference, reducer lifecycle, replay behavior, reduced-motion override, static fallback, scoring, and persistence semantics. Exact next task: obtain approval for the complete architecture/calibration/testing design, then write and self-review `docs/superpowers/specs/2026-08-05-expanded-road-motion-design.md`, commit the spec only, request written-spec approval, and transition to `superpowers:writing-plans`.

Expanded road-motion design checkpoint 3, 2026-08-05: Jeffrey approved the complete behavior, architecture, calibration, fallback, and verification design. The written specification is `docs/superpowers/specs/2026-08-05-expanded-road-motion-design.md`. Self-review found no placeholders, contradictory lifecycle rules, unrelated scope, or unresolved requirements. It fixes the six-second lifecycle and baseline profile values for junction, both roundabout variants, U-turn, overtaking, parking, and voluntary stopping; small browser/iPad calibration adjustments are allowed only to preserve visible, registered targets. No production code has changed. Exact next task: commit the specification and this recovery checkpoint only, ask Jeffrey to review the written spec, then invoke `superpowers:writing-plans` after written-spec approval.

Expanded road-motion implementation checkpoint 1, 2026-08-05: the approved design and recovery ledger were committed as `db41db8` and pushed to `origin/main`. The complete TDD implementation plan is uncommitted at `docs/superpowers/plans/2026-08-05-expanded-road-motion.md`. It decomposes the work into four independently gated tasks: generic scene-calibrated road-motion domain; app-wide audio/answer/replay lifecycle; shared spatial and manoeuvre transform renderers with generic CSS; and release/browser/iPad acceptance plus rollback documentation. Plan self-review reconciled the motion view and renderer interface by explicitly carrying both current `scale` and profile `endScale`. No production code has changed since `7d85fa9`. Exact next task: execute Task 1 test-first, replacing `junction-motion.js` with `road-motion.js` while retaining every accepted lifecycle case and adding the seven exact profile calibrations.

Expanded road-motion implementation checkpoint 2, 2026-08-05: Task 1 is complete and uncommitted. `src/road-motion.js` now provides the immutable six-second lifecycle plus exact scene profiles for the junction, both roundabout photographs, U-turn, overtaking, parking, and voluntary stopping. Its view carries current scale, end scale, and focal origin without mutating inputs. Missing-module RED evidence was observed; the new domain suite passes 8/8, the compatibility/runtime focused gate passes 18/18, and the full repository passes 473/473. One plan audit failure was traced to a credential-header example inside the plan itself; removing the unsafe example restored the existing release audit without runtime changes. The legacy junction module intentionally remains until the app migration so this checkpoint stays green. Exact next task: Task 2 test-first, migrate `src/app.js` from `junctionMotion` to `roadMotion` and enable it from the generated scene profile for all seven approved photo scenes.

Expanded road-motion implementation checkpoint 3, 2026-08-05: Task 2 is complete and Task 3 is at its renderer/CSS GREEN-verification boundary, all uncommitted. `src/app.js` now owns one generic `roadMotion` lifecycle for junction, four- and five-exit roundabouts, U-turn, overtaking, parking, and voluntary stopping; the original six-second audio, unlock, wait, answer, timeout, replay, reduced-motion, and static-fallback semantics remain. The obsolete `src/junction-motion.js` and its test are deleted. A release-audit helper now ignores tracked paths deleted in the working tree instead of trying to read them; the 95-test app/domain/release focused gate passes. Renderer RED evidence was observed for roundabouts and all four manoeuvre families. `src/spatial-surfaces.js`, `src/manoeuvre-surfaces.js`, and `styles.css` now use a shared `road-motion-*` markup/CSS contract with per-scene end scale and focal origin, and keep result/restriction labels outside the transformed viewport. Exact next task: run the three renderer suites, repair only evidenced failures, then run the full suite before beginning release documentation and browser/iPad review.

Expanded road-motion and ElevenLabs recovery checkpoint 4, 2026-08-05: Tasks 1–3 of `docs/superpowers/plans/2026-08-05-expanded-road-motion.md` are complete and uncommitted. The three renderer suites pass, and the full repository suite passes 469/469 with clean `git diff --check`. The generic `road-motion-*` wrapper is active for junction, both roundabout photographs, U-turn, overtaking, parking, and voluntary stopping; each uses the exact approved scale/origin profile. Task 4 browser review has started at `http://127.0.0.1:4175/` with a temporary 1180×820 iPad-landscape viewport. The local server is process/session `81642`. A stale interrupted session was discarded; the browser is currently on setup with Road movement On, before configuring Driving / Free practice / 15 commands. Exact motion next step: select setup controls using their stable option values (`phase=driving`, practice order `free`, length `all`), start, review every supported family for target visibility/overflow/freeze/reveal, verify Road movement Off and reduced-motion static fallbacks, then update release docs, run `npm run release:check`, and record final browser evidence. No commit or push authorization currently remains beyond the already completed `db41db8` spec push.

ElevenLabs expiring-credit workstream, 2026-08-05: Jeffrey expects to cancel at renewal and authorized using remaining credits after the motion build. The production corpus is 36 commands / 76 Spanish phrasings / two voices / three speeds = 456 clips and 16.4 MB. The Spanish text totals 2,149 characters; one complete third voice at all three speeds is approximately 6,447 TTS credits, 228 clips, and 8.2 MB at the current one-credit-per-character model before any provider/model multiplier. Jeffrey approved: (1) create a small Spain-Spanish third-examiner audition pack first, using roughly eight representative commands across short directions, roundabouts, and long prechecks; (2) after voice selection, generate the full third-voice corpus; (3) evaluate a separate harder/brisk delivery corpus; (4) generate a small set of reusable road/cabin-noise assets now, but keep them isolated until later product approval; and (5) evaluate whether ElevenLabs image/video generation offers material value over existing game assets. Do not generate paid assets without first resolving current voice candidates and keeping credentials in the existing Keychain/env-var workflow. Never add speculative clips or manifest entries to production. Context7 confirmed current ElevenLabs TTS, pronunciation dictionaries, Sound Effects v2 (0.5–30 seconds, optional loop), and agent capabilities; it also confirmed rollover requires remaining on the same plan, so these credits are expected to expire on cancellation. Exact ElevenLabs next step after motion acceptance: inspect the existing generator/audition conventions, query the account subscription/model metadata without exposing the key, build an ignored/recovery-safe audition plan, generate and audibly review the small candidate pack, then ask Jeffrey to select the third voice before creating the 228 production clips. Hard-mode, noise, image, and video assets stay outside runtime and Git until separately reviewed.

Expanded road-motion browser blocker checkpoint 5, 2026-08-05: live iPad-landscape review found that the exact approved endpoint scales conflict with the invariant that every registered response remain visible. The five-exit roundabout and parking scenes otherwise passed: calibrated profiles were applied, choices unlocked, reveals froze, result labels remained outside the viewport, target sizes stayed above 44 px, and no horizontal overflow occurred. However, the parking screenshot showed the upper crosswalk target partially clipped after the `1.18 / 65% 84%` push. A 64-seed geometric audit then proved endpoint overflow for six profiles: junction 19.59%, U-turn 13.74%, four-exit roundabout 14.02%, five-exit roundabout 14.03%, stopping 7.85%, and parking 7.83%; overtaking at `1.18 / 54% 86%` has zero overflow. With the current origins, maximum scales that keep the full 44px-capable target boxes inside are approximately junction 1.0816, U-turn 1.0657, both roundabouts 1.038, stopping 1.0792, parking 1.0793, and overtaking 1.4875. This is a design tradeoff rather than a coding defect: strong zoom necessarily crops road mouths spanning almost the full photograph. Recommendation awaiting Jeffrey: preserve target clarity and reduce endpoints to conservative values of junction 1.06, U-turn 1.05, both roundabouts 1.03, stopping 1.06, parking 1.06, while retaining overtaking 1.18 and all focal origins. Exact next task after approval: update the profile registry and expected-profile tests, add a regression sweep proving transformed bounds stay inside the viewport, rerun the full suite/release gate, and repeat the two representative browser checks. Do not proceed to ElevenLabs generation until this motion decision and release review are complete.

Expanded road-motion implementation checkpoint 6, 2026-08-05: Jeffrey approved the target-safe calibration recommendation. TDD RED evidence showed the old scales failing the new profile assertions and clipping sweep. The registry now uses junction 1.06, U-turn 1.05, both roundabouts 1.03, stopping 1.06, parking 1.06, and retains overtaking 1.18 with every original focal origin. A new test generates every eligible command across 64 seeds and proves all transformed target bounds remain inside the endpoint viewport. The road-motion, spatial-renderer, and manoeuvre-renderer focused gate passes 43/43. README, design, changelog, and the execution plan now describe the expanded experiment and its bounded calibrations. Exact next task: run the full automated release gates and clean-diff checks, repeat representative live landscape checks for roundabout and parking plus static Road movement Off, then prepare the recovery-safe third-voice audition pack without publishing speculative assets or changing the production audio manifest.

Expanded road-motion implementation checkpoint 7, 2026-08-05: the implementation is ready for Jeffrey's physical-iPad review and remains uncommitted. `npm run release:check` passes 470/470 tests, builds the deterministic runtime with 506 assets / 20,289,663 bytes and a complete recorded corpus, and finishes with a clean diff check. A live 1180x820 landscape session exercised five-exit and four-exit roundabouts, parallel parking, voluntary stopping, U-turn, junction, and overtaking. All reviewed endpoint targets remained inside their 492x328 transformed viewport; Road movement Off rendered a static four-way junction with no motion wrapper; browser warning/error logs were empty; and the bilingual AI-voice disclosure remained visible. The rollback boundary remains the single uncommitted expanded-motion diff after pushed spec commit `db41db8`. Physical iPad acceptance is the only unchecked plan step. Exact motion next task: Jeffrey reviews the current build on iPad and either approves or requests calibration changes; do not commit without new explicit authorization.

Cruise clips review fixes checkpoint 0, 2026-08-14: baseline at `a7eba0ac6c466030e8464e80164520bb47c40338` (`a7eba0a`, also merge-base with `main`; plan comparison boundary `64fac4b`), with protected untracked `assets/.DS_Store`, `tmp/`, and supplied plan `docs/superpowers/plans/2026-08-14-cruise-clips-review-fixes.md` preserved. `npm test` passed 800/800; `git diff --check` was clean. File map is bounded to Task 1: `src/app.js`, `src/surfaces.js`, `src/manoeuvre-surfaces.js`, `src/spatial-surfaces.js`, and focused renderer/controller/post-answer/turn-through tests; Task 2: `src/offline-client.js` and `tests/offline-client.test.js`. Current matrix: renderers suppress correct static routes solely by registered clip membership, while controller animation requires correct immediate motion conditions; this leaves Road movement Off, reduced motion, deferred session-end reveal, and later clip failure vulnerable to a missing static route. `offline-client` publishes every reply/progress message without command ordering, so a deferred download can overwrite a newer cancellation. Remaining work: write and demonstrate the two focused RED regressions, then make the bounded controller and client guards. Rollback boundary: this checkpoint only; no production change yet.

Cruise clips review fixes checkpoint 1, 2026-08-14: Task 1 is complete and green. Changed `src/app.js`, `src/manoeuvre-surfaces.js`, `src/spatial-surfaces.js`, `tests/app-state.test.js`, `tests/manoeuvre-surfaces.test.js`, and `tests/spatial-surfaces.test.js`. RED evidence: the new renderer contracts failed because renderers inferred from registry membership and the policy helper did not exist. `node --test tests/app-state.test.js tests/manoeuvre-surfaces.test.js tests/spatial-surfaces.test.js tests/post-answer-motion.test.js tests/turn-through.test.js` now passes 148/148. `turnClipWillDemonstrateReveal` is the sole controller policy for a playable reveal transition; it requires immediate correct reveal, a following continuity transition, Road movement On, no reduced motion/session-end policy/session clip failure, and registry membership. Renderers receive only `turnClipWillPlay`; post-answer CSS and auto-advance reuse the same policy. Remaining work: Task 2 controlled out-of-order offline regressions and bounded client generation/progress guards, then integrated verification. Rollback boundary: revert this Task 1 source/test set and restore the renderer predicates; no persisted state, IDs, assets, catalog, or protocol changed.

Cruise clips review fixes checkpoint 2, 2026-08-14: Task 2 is complete and green. Changed `src/offline-client.js` and `tests/offline-client.test.js`. RED evidence: controlled, timer-free requests showed a cancellation replaced by stale failed (`failed`) and successful (`ready`) download replies; late `OFFLINE_PROGRESS` could restore downloading. The fixture now holds individual replies explicitly. `node --test tests/offline-client.test.js tests/offline-cache.test.js tests/service-worker.test.js` passes 26/26. `offline-client` adds a client-local monotonically increasing generation for download, cancellation, update check, and update application; only the current operation publishes a reply, stale calls return the current frozen snapshot, and progress requires the active download generation plus `downloading` state. The intentionally unbounded download reply and all worker/cache protocol remain unchanged. Remaining work: Task 3 focused/full verification, clean diff, credential/disclosure audit, and local manual checks. Rollback boundary: revert Task 2 client/test changes; no worker cache, stored package, protocol, dependency, or persisted-data rollback is needed.

Cruise clips review fixes checkpoint 3, 2026-08-14: integrated verification is complete and the uncommitted repair is ready for Jeffrey review. Focused combined gate `node --test tests/app-state.test.js tests/app-smoke.test.js tests/manoeuvre-surfaces.test.js tests/spatial-surfaces.test.js tests/post-answer-motion.test.js tests/turn-through.test.js tests/offline-client.test.js tests/offline-cache.test.js tests/service-worker.test.js` passes 220/220. `npm test` passes 803/803. Because runtime source inputs changed, `npm run release:check` passed: 803/803 tests, deterministic runtime build `800c12f007c4be55aab7a646b880182f91eeda359ee909a10037c241105e2a59`, 1,301 assets / 61,184,778 bytes / recorded corpus complete, and clean `git diff --check`; the final repeat `git diff --check` is also clean. No interface copy changed, so bilingual-copy parity is unaffected. The local browser title/setup check confirmed the visible English AI-voice disclosure and produced no warning/error logs; production-like static fallback, failed-clip, and delayed cancellation paths are exercised deterministically by the new controller/renderer and controlled-worker tests. Physical Safari/iPad autoplay/decode/end-frame behavior and a hands-on traversal of all six clips remain the residual device acceptance limitation. Diff credential scan found no added credential-shaped value, and the release-audit suite passed. Protected untracked `assets/.DS_Store`, `tmp/`, and the supplied plan remain intact. Rollback boundary: revert the Task 1/2 source and focused-test changes plus these checkpoints; no IDs, persisted data, assets, worker protocol, cache state, dependencies, commit, or push changed.

ElevenLabs expiring-credit checkpoint 2, 2026-08-05: no provider audio was generated and no credits were consumed. Both the voice-catalog request and the minimal `/v1/user` authentication check returned HTTP 401 using the Keychain entry `examen-practico-elevenlabs`, so the saved key is revoked, expired, or otherwise invalid. Production audio, `data/audio-manifest.json`, and runtime assets are unchanged. Exact next task: finish the ignored eight-command audition tooling with per-file reuse, then Jeffrey must create a fresh restricted Text-to-Speech key and replace the same Keychain entry. After authentication returns 200, retrieve current Spain-Spanish candidate voice IDs, generate the small ignored audition pack, and ask Jeffrey to select a voice before any full 228-clip third-voice run. Hard-mode, road/cabin SFX, and image/video experiments remain isolated and unapproved for runtime publication.

ElevenLabs expiring-credit checkpoint 3, 2026-08-05: the ignored audition workflow is complete and uncommitted. `scripts/audition-tts.mjs` now exercises eight representative canonical Spanish commands (`c-der`, `c-recto`, `c-rot3`, `c-rot5`, `c-est`, `c-adapte`, `c-pre-capo`, and `c-pre-desempanar-delantera`) and reuses every existing nonempty output MP3, so provider failures or account handoffs can resume without paying again for completed files. No credential, audio file, production manifest entry, or runtime asset was added. Focused audition tests pass 5/5 and the complete repository suite passes 471/471. The remaining blocker is external: the Keychain-held ElevenLabs key still returns HTTP 401. Exact next task after Jeffrey replaces that key under the same `examen-practico-elevenlabs` Keychain service: verify `/v1/user` returns 200, retrieve current Spain-Spanish candidate voices, generate the isolated resumable audition folders, and stop for Jeffrey's voice selection before generating the 228-clip third-voice corpus. The expanded road-motion implementation remains independently release-ready and awaits only physical-iPad review/commit authorization.

ElevenLabs expiring-credit checkpoint 4, 2026-08-05: the provider experiment is fully documented for a same-machine account handoff in `references/elevenlabs-expiring-credit-plan.md`. It records the eight-command third-voice audition, the post-selection 228-clip estimate, a later brisk-delivery audition, two isolated 12-second seamless road/cabin ambience prompts, and the current recommendation against spending credits on beta image/video generation. Official documentation currently quotes 11 API credits per second for explicitly timed sound effects, making the two first-pass ambience loops approximately 264 credits total. No speculative asset has been generated, published, or added to the runtime. The sole operational blocker remains replacement of the invalid Keychain-held API key.

ElevenLabs permission correction checkpoint 5, 2026-08-05: the Keychain-held API key is not proven invalid. Safe diagnostic requests established that it is a 51-character whitespace-free key, and ElevenLabs returned explicit permission errors: `/v1/user` lacks `user_read` and `/v2/voices` lacks `voices_read`. The HTTP 401 responses in checkpoints 2–4 were therefore permission failures caused by the deliberately restricted key, not evidence that switching ChatGPT accounts affected ElevenLabs authentication. Text-to-Speech may still work under its original scope. Exact unblock: in ElevenLabs, edit the key or create a replacement with Text to Speech access plus Voices read access for catalog-based audition selection; add Sound Effects access only when generating the separately approved ambience pack. `user_read` is useful for a no-cost account/quota diagnostic but is not required for TTS generation. Store any replacement under the same macOS Keychain service; never put it in Git or a browser-delivered file.

ElevenLabs access checkpoint 6, 2026-08-05: Jeffrey expanded the permissions on the existing Keychain-held API key. No-cost verification now succeeds for `/v1/user` and `/v2/voices`; the Starter subscription reports 28,081 of 30,025 credits remaining. A deliberately incomplete Sound Effects request reached request validation and returned HTTP 422 for the missing `text` field, confirming Sound Effects permission without generating an asset or consuming credits. The provider blocker is cleared. Exact next task: shortlist current Spain-Spanish third-examiner voices from the catalog, generate the isolated resumable eight-command audition folders at 1x, and present the clips to Jeffrey before any full-corpus generation.

ElevenLabs audition checkpoint 7, 2026-08-05: the current 21-voice premade catalog was filtered for voices with verified `es-ES` output. Three contrasting candidates were selected: George (`JBFqnCBsd6RMkjVDRZzb`, warm male), Matilda (`XrExE9yKIg1WjnnlVkGX`, professional alto), and Eric (`cjVigY5qzO86Huf0OWal`, smooth middle-aged male). The resumable runner generated all eight approved 1x Spanish audition clips for each candidate, 24/24 nonempty MP3s total, under ignored directories `tmp/audio-audition/george`, `tmp/audio-audition/matilda`, and `tmp/audio-audition/eric`. Production audio and the manifest remain unchanged. The immediately reported account balance remained 28,081 credits, though provider usage reporting may lag. Exact next step: Jeffrey listens to the three packs and selects a voice (or rejects all three); only after selection may the 228-clip full third-voice corpus be generated.

Five-voice production checkpoint 8, 2026-08-05: Jeffrey liked George, Matilda, and Eric and explicitly approved generating all three complete corpora. The safe production plan must include all five voice IDs in one generator invocation—Roger `CwhRBWXzGAHq8TQ4Fs17`, Sarah `EXAVITQu4vr4xnSDxMaL`, George `JBFqnCBsd6RMkjVDRZzb`, Matilda `XrExE9yKIg1WjnnlVkGX`, and Eric `cjVigY5qzO86Huf0OWal`—because `generate-audio.mjs` publishes exactly the passed voice set. The target is 1,140 variants (76 phrasings × 5 voices × 3 speeds): reuse the 456 published Roger/Sarah files and generate 684 new files. Expected new TTS usage is roughly 19,341 billed characters before provider multiplier, within the 28,081-credit balance. Every generated file and manifest record checkpoints under `.superpowers/sdd/audio-expansion-recovery/audio-expansion/`; an interruption is resumed by rerunning the exact five-voice command. Do not update counts/tests/docs or treat the new corpus as published until the generator completes its integrity validation and atomic publication.

Five-voice production checkpoint 9, 2026-08-05: the first five-voice run stopped safely when ElevenLabs returned HTTP 401 at planned record 939/1,140 (`c-pre-desbloquear-elevalunas` canonical, George, 1x). Account access still returns HTTP 200 and reports 20,150 credits remaining, so this was not subscription exhaustion. The recovery manifest contains 560 verified new records: George 188/228, Matilda 186/228, and Eric 186/228. The original 456-file production corpus remains untouched because atomic publication has not run. Exact recovery action: rerun the same five-voice generator command; it will verify/reuse all 456 production records plus these 560 recovery records, then request only the remaining 124 new clips. If HTTP 401 repeats on the same variant, capture the provider error body before changing permissions or text; do not delete recovery state.

Five-voice production blocker checkpoint 10, 2026-08-05: a second recovery run reused all prior work and failed again on the same George 1x variant. A direct diagnostic request captured the exact provider response: the API key `practice-audition-3` has a 10,000-credit key quota, only 5 key-level credits remain, and the next request needs 28. The ElevenLabs account itself still has approximately 20,150 credits; only the per-key ceiling is blocking generation. The recovery ledger remains at 560 new clips, with 124 clips / 3,992 Spanish characters left. Exact unblock: Jeffrey edits the existing key and raises its quota from 10,000 to at least 14,000 total; 16,000 or unlimited is safer. Then rerun the same five-voice command. No production audio or manifest has been replaced, and no completed work needs regeneration.

Five-voice production completion checkpoint 11, 2026-08-05: Jeffrey raised the existing ElevenLabs key quota to 20,000 and the identical recovery-safe five-voice generator run completed. Atomic publication succeeded with 1,140 integrity-checked MP3 variants: 76 Spanish phrasings × five voices × three provider-native speeds, exactly 228 recordings for each of Roger, Sarah, George, Matilda, and Eric. The run reused the previous 456 recordings and added 684; the published audio tree is 40,062,173 bytes (38.21 MiB), the recovery directory was removed after successful publication, and ElevenLabs reported 18,327 account credits remaining. Active documentation, release audits, generator tests, and offline-package completeness now use the five-voice corpus. `npm run release:check` passes all 471 tests, completes `git diff --check`, and builds deterministic runtime package `2351fea14a4843d3d26ee4fc35470b0a07d28ff370f6e1e7b2f4e996194ab533` with 1,190 assets / 44,312,899 bytes / `recordedCorpusComplete: true`. The expanded road-motion and five-voice audio changes remain uncommitted. Physical-iPad review of expanded motion and the larger offline download remains pending; hard-mode and ambience assets remain isolated follow-up experiments, not runtime content.

ElevenLabs delivery experiments checkpoint 12, 2026-08-05: the separately approved, review-only hard-mode and ambience auditions are generated and remain ignored outside runtime. `scripts/audition-tts.mjs` accepts an audition-only 1.15x speed without changing the production speed set; it generated the eight representative commands for all five voices, 40/40 nonempty MP3s totaling 1,100,559 bytes, plus `tmp/audio-audition/Hard-mode-five-voice-audition.zip`. Sound Effects v2 generated two seamless 12.04-second 44.1 kHz stereo loops—Spanish city-car interior and rural-road interior—at 193,141 bytes each, plus `tmp/audio-ambience/Driving-ambience-audition.zip`. The ambience pair moved the ElevenLabs account counter by 542 credits, more than the documentation-derived estimate; the later hard-mode balance query still reported 17,785 remaining, likely due provider reporting lag. Neither experiment changes scoring, the production manifest, or the offline package. Exact next decision after listening: reject, revise, or separately design a hard-mode/ambience product option; do not publish either automatically.

Solo engagement roadmap checkpoint 13, 2026-08-06: Jeffrey explicitly realigned product priority toward making solo practice physical, challenging, and fun before a full native port or any social platform. The proposed roadmap stages examiner identity and Learn/Practice/Mock presets, themed drives and continuous mock pacing, sensory/physical consequence, self-selected challenge modes, competence-linked accomplishments, and optional listening-game formats. The five voices become restrained recurring examiners rather than caricatures. The old categorical rejection of engagement is narrowed: manipulative retention stays excluded, while evidence-backed challenge and accomplishment are permitted. Pass-the-iPad, shareable challenges, cooperative goals, host mode, lesson debriefs, and reviewed community packs are preserved in a deferred social backlog. Release D remains gated on real lessons. Exact next decision: Jeffrey reviews the roadmap; after approval, write one implementation specification for Solo E1 and E2 together. Do not publish hard-mode audio, ambience, achievements, or social features in that first build.

Solo engagement estimation checkpoint 14, 2026-08-06: the roadmap now includes piece-level clock, frontier-token, and ownership estimates for E1–E6. The initial E1+E2 release is estimated at 6–11 focused agent-days and roughly 225k–415k primary-model tokens if Codex performs everything, or 140k–265k with bounded Hermes delegation. Hermes is recommended for preset/theme domain modules, examiner and route presentation, bilingual UI, fixtures, and mechanical audio/package work after contracts stabilize. Codex retains active-session transitions, withheld-reveal scoring, persistence and migration, audio lifecycle, motion/accessibility judgment, full-suite integration, and physical review. E1–E5 together remain a multi-release 21–38 day program rather than one continuous build. Exact next step remains roadmap approval followed by a written E1+E2 specification that identifies the first Hermes work packages before implementation begins.

Solo E1 overnight checkpoint 15, 2026-08-06: Jeffrey approved the solo-engagement roadmap and authorized a bounded overnight foundation with recovery checkpoints. The existing uncommitted expanded-road-motion and five-voice production release remains the rollback boundary; do not modify or discard it. The new design is `docs/superpowers/specs/2026-08-06-examiner-modes-themed-drives-design.md` and the task plan is `docs/superpowers/plans/2026-08-06-examiner-modes-foundation.md`. Tonight may create only pure examiner/preset modules, tests, i18n additions, an unintegrated semantic UI scaffold, and future Hermes prompts. It must not modify storage schemas, active sessions, scoring, `src/app.js`, production audio, offline packaging, or deployment. Approved foundation semantics: Learn = shown Spanish/unlimited replay/immediate reveal; Practice = current fresh-save behavior; Mock = Spanish unavailable/timed/1x/no replay/deferred reveal, with no pass threshold yet. Exact next task: write RED tests for the five-examiner registry, deterministic local-date Today choice, fixed/Mixed candidate filtering, and immutability, then implement `src/examiners.js`.

Solo E1 overnight checkpoint 16, 2026-08-06: examiner-domain Task 1 is GREEN and unintegrated. `src/examiners.js` defines exactly Roger, Sarah, George, Matilda, and Eric against their five published ElevenLabs voice IDs, plus stable IDs, localization keys, and CSS-safe visual tokens. It validates registry uniqueness, performs exact ID/voice lookup, deterministically rotates Today's examiner from injected local calendar parts, filters cloned/frozen audio candidates for Mixed, Today, or one fixed examiner, and reports missing production voices. `tests/examiners.test.js` passes 11/11 including malformed-date, unknown-choice, coverage, immutability, and no-mutation cases. No production controller, persistence, manifest, audio, or UI file imports this module. Exact next task: write RED tests for immutable Learn/Practice/Mock preset records and base-setting application, then implement `src/session-presets.js` without changing stored settings.

Solo E1 overnight checkpoint 17, 2026-08-06: experience-preset Task 2 is GREEN and unintegrated. `src/session-presets.js` defines immutable Learn, Practice, and Mock contracts with the approved phase, speed, Spanish-hint, timing, length, ordering, replay, reveal, and simulation semantics. Applying a preset immutably overrides only preset-owned settings, preserves unrelated and future additive fields, and validates both caller settings and alternate registries. Practice is regression-tested against fresh-save production defaults for every field it owns. `tests/session-presets.test.js` passes 8/8; the combined examiner/preset gate passes 19/19. No saved schema, active session, controller, scoring, manifest, audio, package, or service-worker behavior changed. Exact next task: write RED tests and implement the bilingual semantic `src/solo-setup-view.js` scaffold plus i18n keys, without importing it from `src/app.js` or adding production CSS.

Solo E1 overnight checkpoint 18, 2026-08-06: bilingual future-UI Task 3 is GREEN and remains unintegrated. `src/solo-setup-view.js` renders two semantic radio fieldsets for Learn/Practice/Mock and Today/Mixed/five fixed examiners, exposes stable future controller data attributes, visibly identifies selected choices, resolves Today's examiner from injected local date parts, and escapes all dynamic markup and attribute content. `src/i18n.js` contains symmetric English/Spanish mode, examiner, neutral delivery-description, simulated-format, and selection copy; no regional, temperament, or official-difficulty biography was invented. The production controller neither imports nor calls this scaffold, and production CSS is unchanged. RED evidence was the expected missing-module failure; the focused examiner/preset/view/i18n gate now passes 45/45. Exact next task: write the two bounded future Hermes handoff prompts, run the complete suite and `git diff --check`, then stop for Jeffrey's foundation review without commit, push, deployment, persistence, or live integration.

Solo E1 foundation completion checkpoint 19, 2026-08-06: all four bounded plan tasks are complete and uncommitted. The five-examiner registry, deterministic Today/Mixed/fixed filtering, Learn/Practice/Mock preset contracts, and escaped bilingual semantic setup scaffold are implemented as pure modules; the renderer remains absent from `src/app.js`, and no storage, active-session, scoring, production-audio, package, service-worker, CSS, or deployment integration was performed. Future bounded review and E2 theme-domain prompts are recorded in `docs/superpowers/plans/2026-08-06-hermes-solo-e1-domain-handoff.md` and `docs/superpowers/plans/2026-08-06-hermes-solo-e2-theme-handoff.md`; the latter explicitly requires fresh Jeffrey approval before use. Final focused gate passes 45/45 and the complete repository suite passes 499/499. `git diff --check` is clean. Existing expanded-motion and five-voice production changes remain preserved in the shared dirty worktree. Exact next decision: Jeffrey reviews the foundation contracts and neutral examiner copy; only then write the live-integration specification covering schema migration, Today snapshots, replay/reveal enforcement, deferred Mock diagnostics, resume, focus, offline packaging, and physical-iPad acceptance.

Solo E1 Spanish-name checkpoint 20, 2026-08-06: Jeffrey approved the foundation documents subject to checking that the examiner character names read naturally as Spanish names. The visible character names are now exactly Roger, Sara, Jorge, Matilde, and Eric. Stable internal examiner IDs (`roger`, `sarah`, `george`, `matilda`, `eric`), ElevenLabs voice IDs, provider voice names, audio paths, and manifest records remain unchanged. Test-first RED evidence was two expected failures while the implementation still exposed Sarah, George, and Matilda; after the display-only correction, the focused examiner/preset/view/i18n gate passes 45/45, the complete repository suite passes 499/499, and `git diff --check` is clean. The E1 Hermes review prompt now explicitly audits both the visible Spanish names and the unchanged provider identities. Exact next task: run that bounded independent E1 review in a clean Hermes worktree; do not perform live integration or start the E2 theme build until Codex reviews the returned findings.

Solo E1 independent-review checkpoint 21, 2026-08-06: Hermes completed the bounded read-only review with no findings. It reported the focused gate and the complete 499/499 suite passing, and `git diff --check` clean; Codex independently reran the focused gate at 45/45 and the diff check successfully. Hermes's phrase “working tree clean” was imprecise: the shared checkout still has 270 expected modified/untracked status entries from the preserved expanded-motion, five-voice, and Solo E1 work, while `git diff --check` establishes only that the diff has no whitespace errors. No E1 repair or live integration is required. Jeffrey's earlier approval to continue outsourcing now unblocks only the pure E2 theme foundation. Exact next task: run `docs/superpowers/plans/2026-08-06-hermes-solo-e2-theme-handoff.md` with Nemotron Super in the current checkout, permitting writes only to new `src/session-themes.js` and `tests/session-themes.test.js`; Codex must review the returned implementation before any integration.

Solo E2 theme-foundation checkpoint 22, 2026-08-06: Hermes's pure theme implementation has been repaired and independently reviewed. `src/session-themes.js` now provides the six approved immutable theme records and deterministic, duplicate-free selection; tests enforce deep caller-independent freezing, exact stable command composition, use of the first injected RNG draw, validation of every RNG result, and duplicate-command-ID rejection. A bounded read-only Hermes audit ran successfully through OpenRouter with Nemotron 3 Super in session `20260806_200427_78b695` and returned no findings. Codex independently verified the focused E1/E2/i18n gate at 64/64 and `git diff --check` clean. The large shared worktree remains intentionally dirty with protected expanded-motion, five-voice audio, E1, and E2 foundation changes. No live controller, persistence, scoring, packaging, commit, or push occurred. Exact next task: inspect the current storage, active-session, training, playback, and controller boundaries and draft the review-gated E1+E2 live-integration specification; do not integrate before Jeffrey approves that specification.

Solo E1+E2 live-integration design checkpoint 23, 2026-08-06: direct inspection of `src/storage.js`, `src/active-session.js`, `src/training.js`, `src/audio.js`, `src/variant-coverage.js`, and the production controller established the current integration boundaries. In particular, coverage-aware phrasing/voice selection is already live through `src/app.js:selectPlaybackVariant`; an OpenRouter North Mini Code reconnaissance session incorrectly reported that it was not integrated, so its file inventory was retained but its architectural conclusions were not accepted. A requested second independent Ultra audit did not yield a distinct saved session—the exported Hermes record resolved to the same North Mini Code run—so no false corroboration is claimed. The resulting review-gated design is `docs/superpowers/specs/2026-08-06-examiner-modes-themed-drives-live-integration-design.md`. It proposes additive schema 4 settings, an immutable active-session v2 experience snapshot, theme eligibility before the existing readiness-aware selector, examiner filtering before the existing coverage-aware audio selector, unchanged immediate Learn/Practice reveal, and persisted-but-withheld Mock evidence reconstructed at session end. No production integration occurred. Exact next gate: Jeffrey reviews and approves or changes the five decisions at the end of that design; only after approval may an implementation plan or live code changes begin.

Solo E1+E2 live-integration plan checkpoint 24, 2026-08-06: Jeffrey approved all five decisions in the live-integration design. The executable plan is `docs/superpowers/plans/2026-08-06-examiner-modes-themed-drives-live-integration.md`; it divides the build into ten independently gated tasks covering schema 4, active-session v2, theme eligibility, examiner-filtered coverage-aware playback, setup integration, Learn/Practice regression protection, Mock withholding, completed-Mock recovery, identity presentation, and release/iPad acceptance. The complete pre-implementation suite passes 518/518 and `git diff --check` is clean. No live integration code has changed yet. Exact next task: delegate the bounded schema-4 migration to Hermes with writes restricted to `src/storage.js` and its focused tests, then have Codex review the diff and rerun the gate before Task 2. If the free model is rate limited, preserve its session ID and filesystem/test state and resume the same slice rather than restarting or broadening it.

Solo E1+E2 live-integration checkpoint 25, 2026-08-06: Task 1, the additive schema-4 migration, is GREEN and uncommitted. Hermes's fenced free-model attempt touched only `src/storage.js` and `tests/storage.test.js` but entered a repeated test-repair loop; Codex stopped it, removed an over-broad schema-1 rewrite, derived experience/examiner/theme validation from the reviewed stable registries, and completed the migration directly. Fresh saves now default to Practice, Mixed, and no theme; schema-1/schema-2/schema-3 saves migrate sequentially without mutating input or losing attempts, progress, lesson flags, or active sessions; schema 4 rejects unknown stable IDs while retaining additive data. The Release B design and release-audit assertion now identify schema 4. Focused storage/release tests pass 40/40, the complete suite passes 520/520, and `git diff --check` is clean. Jeffrey has separately approved sending relevant public-repository files to Claude Code for bounded delegated work and review; every Claude task must remain file-fenced, test-gated, checkpointed, and commit/push-free, with its session retained for interrupted recovery. Exact next task: implement active-session version 2 and its immutable experience/examiner/theme snapshot under Task 2 of `docs/superpowers/plans/2026-08-06-examiner-modes-themed-drives-live-integration.md`, then rerun focused and full gates before proceeding.

Solo E1+E2 live-integration checkpoint 26, 2026-08-06: Task 2, the active-session version-2 snapshot, is GREEN and uncommitted. New sessions contain one deeply frozen `experience` record; version-1 sessions normalize immutably to Practice/Mixed/no theme while preserving their exact item, settings, target, index, and attempt IDs. Validation rejects unknown stable mode/examiner/theme values, preset-inconsistent replay/reveal/simulated policies, Learn/Mock speed-hint-timing mismatches, inconsistent Today/fixed identities, and item voice IDs that differ from the resolved examiner. Resolution rejects commands outside a selected theme, returns the experience snapshot, and accepts a completed Mock snapshot for later results reconstruction; the existing app helper still clears completed ordinary sessions. Focused active-session/storage/controller tests pass 41/41, the complete suite passes 526/526, and `git diff --check` is clean. Claude Code CLI OAuth was refreshed successfully, but read-only Sonnet reviews under sessions `a6dc94e8-39f2-480b-8899-595f0ddc3101` and `b6f15ea7-62d8-47df-ab3d-883505745e09` stalled without output and were stopped; the second had only Read/Grep/Glob capabilities and could not change files. Exact next task: Task 3, add a pure frozen theme-eligibility helper in `src/session-themes.js` and compose it before the existing readiness-aware selector in `src/training.js`, with focused tests and no controller/UI changes.

Solo E1+E2 live-integration checkpoint 27, 2026-08-06: Task 3, theme eligibility before adaptive selection, is GREEN and uncommitted. `eligibleCommandsForTheme` returns caller-independent deeply frozen commands in catalog order without shuffling, truncating, padding, duplication, or mutation, and returns a frozen empty array when a valid theme has no matches. `createSession` applies this eligibility boundary before the existing phase, target, readiness, lesson-flag, order, and length selector; no-theme sessions retain prior behavior, while an impossible theme/phase or theme/command-target combination returns an explicit frozen empty result for the future bilingual Start-disabled explanation. Focused theme/training tests pass 36/36, the complete suite passes 530/530, and `git diff --check` is clean. Current implementation status: plan Tasks 1-3 are complete; no setup UI, examiner playback, or Mock lifecycle is live yet. Exact next task is Task 4: resolve Today once at session creation, filter recorded candidates for Today/fixed examiner before the already-live coverage-aware selector, keep Mixed across all five voices, persist the resolved examiner and exact variants, and preserve unscored browser-speech/audio-failure behavior. This is a clean account-handoff boundary; the next account should read `AGENTS.md`, this ledger, the approved integration spec, and the ten-task implementation plan before touching `src/app.js`.

Solo E1+E2 live-integration checkpoint 28, 2026-08-06: Task 4, examiner-filtered coverage-aware playback, is GREEN and uncommitted. `resolveSessionExperience` creates the immutable mode/examiner/theme/policy snapshot and resolves Today exactly once from injected local calendar parts; fixed and Today sessions filter recorded candidates before the existing coverage-aware selector, while Mixed remains an identity filter across all five voices. A missing requested recorded examiner never leaks to another voice or creates an invalid fixed-examiner browser-speech snapshot; the existing browser-speech fallback remains unchanged for genuinely unrecorded Mixed commands, and audio failures remain unscored. Production session construction now applies `themeId`, snapshots exact selected phrasing/voice/speed records, and passes the experience record to active-session v2 for exact resume. Focused examiner/controller tests pass 79/79, the complete suite passes 532/532, and `git diff --check` is clean. Exact next task is Task 5: integrate the reviewed semantic setup view into production with bilingual mode, examiner, Adaptive/theme choices and an Advanced practice disclosure, plus a bilingual disabled-Start reason for empty or unplayable combinations; do not begin Mock lifecycle changes in Task 7 yet.

Solo E1+E2 live-integration checkpoint 29, 2026-08-06: Task 5, the semantic production setup and Start-eligibility boundary, is GREEN and uncommitted. The setup now presents bilingual experience, examiner, and Adaptive/six-theme cards, keeps the technical controls in an Advanced practice disclosure, and disables Start with a localized explanation when the selected theme/phase has no commands or a fixed examiner has no playable recording. The pure setup renderer was partially delegated to Hermes through OpenRouter, but the free Nemotron run stalled after one malformed partial edit; Codex stopped session/process `37867`, preserved the filesystem state, repaired the renderer, and completed the controller, i18n, CSS, and tests locally. Focused Task-5 tests pass 97/97, the complete repository suite passes 535/535, and `git diff --check` is clean. The setup choices are visible, but Learn and Mock execution must not be represented as complete until Task 6 applies preset-owned session settings without rewriting saved Practice preferences, followed by Task 7's Mock withholding lifecycle. Exact next task: implement Task 6 test-first, using active-session settings for runtime timing/hint/speed decisions while retaining Practice behavior exactly.

Solo E1+E2 live-integration checkpoint 30, 2026-08-06: Task 6, the Learn/Practice live regression boundary, is GREEN and uncommitted. `effectiveSessionSettings` creates a frozen per-session copy: Practice preserves every visible advanced control, while Learn applies only its owned 0.9x speed, always-shown Spanish, and untimed policy, leaving phase, length, order, examiner, theme, feedback, and road-motion choices intact. Setup playability checks and audio selection use the effective speed; prompt rendering, fallback playback, attempt timing metadata, and timer startup now use the active model's snapshotted session settings rather than mutable saved preferences. Resume restores the session settings into the live model without rewriting the learner's saved setup preferences. Learn and Practice retain the existing immediate reveal, feedback, diagnosis, lesson-note, persistence, focus, and Continue lifecycle. Focused controller/preset/session tests pass 93/93, the complete repository suite passes 536/536, and `git diff --check` is clean. Exact next task is Task 7: implement Mock's no-replay/no-Spanish/timed 1x neutral transition and session-end withholding lifecycle, retaining atomic attempt persistence and unscored audio-failure resume.

Solo E1+E2 live-integration checkpoint 31, 2026-08-06: Task 7, the in-drive Mock withholding lifecycle, is GREEN and uncommitted. Mock applies 1x audio, unavailable written Spanish, timing on, no Replay, and session-end reveal; the reducer rejects replay and hint events even if dispatched outside the UI. Each response is reduced to evidence and persisted immediately with the active-session index and attempt ID in the same save, but the learner sees no correctness, meaning, expected action, miss diagnosis, or feedback sound. Instead, a bilingual neutral Response recorded frame identifies the experience as simulated and advances after 600 ms to the next unanswered command. The completed Mock active-session snapshot is intentionally retained at `nextIndex === item count` for Task 8 reload-safe reconstruction; ordinary sessions still clear at completion. Focused Mock/controller/i18n tests pass 92/92, the complete repository suite passes 538/538, and `git diff --check` is clean. Exact next task is Task 8: reconstruct completed Mock results from its retained attempt IDs on initial load/resume, render the disclosed clean/needs-practice headline and per-command evidence, then clear the retained snapshot only when results are dismissed or a new session starts.

Solo E1+E2 live-integration checkpoint 32, 2026-08-06: Task 8, completed-Mock recovery and deferred review, is GREEN and uncommitted. A completed Mock retains its version-2 snapshot and exact attempt IDs; choosing Resume after a reload reconstructs the results screen without replaying an answered command. The initial result says Clean simulated drive only when every expected response is unaided and otherwise says Needs practice, alongside a bilingual statement that this is not an official DGT result. The deferred ordered review exposes each exact Spanish phrasing, localized meaning where the existing interface provides it, expected action, outcome, response time, and replay count. Incorrect rows permit a persisted miss-reason update keyed by attempt ID without changing outcome or score. Starting another session replaces the snapshot and dismissing results clears it; ordinary session cleanup is unchanged. Focused persistence/controller/training/i18n tests pass 123/123, the complete repository suite passes 540/540, and `git diff --check` is clean. Exact next task is Task 9: present localized mode, theme, and examiner identity during prompts and results, then audit responsive keyboard/reduced-motion/iPad-landscape layout before the Task-10 release gate.

Solo E1+E2 live-integration checkpoint 33, 2026-08-06: Task 9, session identity and responsive presentation, is GREEN and uncommitted. Prompts, the neutral Mock transition, and results now show a compact bilingual strip for mode, drive theme, and examiner. Fixed/Today sessions use the examiner's approved localized name, neutral description, and visual token; Mixed uses the plural Mixed examiners label, description, and all five tokens rather than presenting one fictional person. A live 1180x820 iPad-landscape check covered a photo-backed parking prompt in English and Spanish: the three identity chips remained in one row above the gameplay layout, the 492x328 target stage was unobstructed, document scroll width/height exactly matched the viewport, and browser warning/error logs were empty. Temporary viewport overrides and review tabs were cleaned up after verification. Focused controller/rendering/i18n tests pass 96/96, the complete repository suite passes 542/542, and `git diff --check` is clean. Exact next task is Task 10: update release bookkeeping, run `npm run release:check`, verify the packaged five-voice corpus and AI disclosure/credential audit, then prepare a LAN or verified-runtime URL for Jeffrey's physical-iPad acceptance. No commit or push has occurred.

Solo E1+E2 release-candidate checkpoint 34, 2026-08-06: Task 10 automated gates are GREEN and the verified runtime is awaiting Jeffrey's physical-iPad acceptance. Public documentation and the changelog now describe Learn/Practice/Mock, five fixed/Today/Mixed examiner choices, six themed drives plus Adaptive, completed-Mock recovery, and the compact session identity strip. `npm run release:check` passes 542/542 tests, rebuilds the deterministic runtime, and leaves `git diff --check` clean. Package hash `8ead227b2f8387ff50960f36ec38e0cabd77520b71fedbe63b111b512e24e95b` contains 1,194 assets / 44,365,272 bytes and reports the 1,140-recorded-variant corpus complete. Release audits cover repository and built-runtime credential-shaped text, bilingual AI-voice disclosure, legacy schema migration and version-1 resume compatibility, Mock withholding/recovery, browser-speech fallback, backup validation, and staged offline updates. The rollback boundary is committed `db41db8`; all examiner-mode/theme work remains uncommitted in the intentionally dirty shared checkout. The verified `dist/` build is served on the LAN at `http://192.168.1.164:4201/` by session 58339. Remaining acceptance: physical iPad landscape review of setup, Learn, Practice, Mock completion/reload, EN/ES identity, and an offline update/download smoke. Deferred product work remains instructor-validated sequential exam ordering, deeper phrase/voice mastery reporting, automatic difficulty progression, and social/multiplayer features.

Solo E1+E2 physical-iPad acceptance checkpoint 35, 2026-08-06: Jeffrey reviewed the verified LAN runtime on the physical iPad and reported that everything appears to work. This accepts the examiner-mode/theme release candidate's landscape setup and gameplay presentation, Learn/Practice/Mock behavior, examiner/theme identity, bilingual interface, and completed-Mock review/resume path. Automated release evidence remains the checkpoint-34 package: 542/542 tests, clean `git diff --check`, hash `8ead227b2f8387ff50960f36ec38e0cabd77520b71fedbe63b111b512e24e95b`, 1,194 assets / 44,365,272 bytes, and a complete 1,140-recorded-variant corpus. Implementation and release bookkeeping remain uncommitted in the intentionally dirty shared checkout; the rollback boundary is `db41db8`. Exact next action is Jeffrey's commit/push decision, followed by the GitHub Pages workflow and optional installed-app offline-update confirmation on the published HTTPS build.

Simulated-exam continuity design checkpoint 36, 2026-08-07: after release commit `3ccc0c3`, Jeffrey proposed turning Full Mock into a continuous simulated exam with prechecks first, an eventual scored start-engine command, departure, short straight-driving bridges between driving commands, and a terminal finish/immobilization sequence. Catalog audit confirms no current start-engine or pull-away command. The review-gated design is `docs/superpowers/specs/2026-08-07-simulated-exam-continuity-design.md`; the executable plan and fenced, rate-limit-recoverable Hermes and Claude prompts are dated `2026-08-07` in `docs/superpowers/plans/`. The low-risk first slice reuses audited road photography and the existing camera push-in, changes only Mock + Full mock, treats transitions as unscored, preserves exact attempt/resume semantics through a future optional active-session-v3 continuity snapshot, and keeps the sequence explicitly simulated. Production implementation is blocked only on Jeffrey's five design decisions. After approval, Hermes may create only the pure route planner/tests/handoff and Claude Code may create only the pure transition renderer/tests/scoped CSS/handoff; Codex retains persistence and controller integration. Untracked `tmp/` remains protected and unrelated.

Simulated-exam continuity approval checkpoint 37, 2026-08-07: Jeffrey approved all five design decisions and added two scored commands. `c-arr` will use canonical `Arranque el motor` plus supplementary `Ponga el motor en marcha`, both mapped to one start-engine response. `c-incorp` will use canonical `Incorpórese a la circulación`, mapped to joining traffic safely. These are review-derived/composite commands, not Fermin-guide claims. They bring the planned catalog to 38 commands / 79 phrasings and require 45 new recordings, yielding 1,185 total variants across five examiners and three speeds. Visual audit confirmed the four-exit roundabout PNG contains an incomplete lower-right road branch inherited from the five-exit composition; the five-exit source is clean. ImageGen produced a non-destructive candidate `assets/driving/roundabout-four-photo-v2.png` at 1536×1024 with only that stump removed; it is not yet referenced by production and needs WebP conversion, alignment tests, browser review, and iPad acceptance. Exact next action: dispatch the fenced Hermes pure route task, then the fenced Claude transition scaffold; Codex reviews both before persistence/controller integration. Untracked `tmp/` remains protected.

Simulated-exam continuity implementation checkpoint 38, 2026-08-07: the fenced pure route planner and bilingual transition view have been completed and reviewed; the catalog now contains 38 commands / 79 Spanish phrasings, including `c-arr` and `c-incorp`; the procedure-neutral `start-engine-v1` semantic surface and bilingual action labels are implemented; and active-session version 3 plus schema-4 round-trip persistence now retain an immutable route cursor while normalizing version-2 sessions with continuity disabled. Generated but unintegrated visual candidates are `assets/driving/join-traffic-photo-v1.png` and the cleaned four-exit `assets/driving/roundabout-four-photo-v2.png`; both retain their current source assets as rollback points. Pending work is controller wiring for Mock + Full mock transitions and restart/skip behavior, the `join-traffic-v1` response surface and departure consequence, 45 Spanish recordings plus atomic manifest publication, and four-exit WebP conversion/reference/target-motion alignment followed by browser and physical-iPad review. Release documentation, package verification, and Jeffrey's commit/push decision remain later gates. Untracked `tmp/` remains protected and unrelated.

Simulated-exam continuity integration checkpoint 39, 2026-08-07: the delegated slices are integrated and remain uncommitted. Mock + Full mock now constructs a deterministic narrative route from the already-selected commands, orders selected prechecks before departure, inserts unscored skippable straight-driving bridges, persists and resumes an immutable continuity cursor, and advances transitions without creating attempts. `start-engine-v1` and the photo-backed `join-traffic-v1` response surfaces are active; the cleaned `roundabout-four-photo-v2` WebP is active while v1 remains as a rollback asset. The complete suite passes 584 tests with one expected skip for the 45 not-yet-recorded Spanish variants, and `git diff --check` is clean. A live 1180x820 Full Mock run confirmed the setup, session identity, cleaned four-exit photograph, and gameplay surfaces render at iPad-landscape dimensions; automated browser audio is unsuitable for listening acceptance. Release bookkeeping now distinguishes the working 38-command / 79-phrasing catalog and 1,185-clip target from the published 1,140-clip corpus. ElevenLabs generation made no changes because the `examen-practico-elevenlabs` Keychain item is unavailable to the current account/session; the exact generation workflow remains resumable once Jeffrey restores that Keychain entry. Remaining gates are generation of the 45 clips and atomic manifest update, release check/build, physical-iPad continuity/audio acceptance, and Jeffrey's commit/push decision. Untracked `tmp/` remains protected and unrelated.

Simulated-exam continuity audio checkpoint 40, 2026-08-07: Jeffrey restored the 52-character `examen-practico-elevenlabs` Keychain entry. ElevenLabs confirmed that the key is valid but lacks the optional `user_read` permission; Text to Speech access succeeded. The resumable generator reused all 1,140 existing production variants, created exactly 45 new Spanish MP3s for `c-arr` and `c-incorp` across five examiners and three speeds, and atomically published a complete 1,185-entry manifest. Post-generation bookkeeping now records 38 commands / 79 phrasings / 1,185 recordings with zero missing. `npm run release:check` passes all 585 tests, builds a deterministic 1,244-asset / 46,249,794-byte runtime with package hash `346e48cf1eb0f5c75549134276e5fba5c1b02e2a9b2d780ee2f225e7578e0c3a`, reports `Recorded corpus complete: true`, and finishes with a clean diff check. `user_read` is not required for production TTS; enable it only for account/quota inspection, retain `voices_read` for future auditions, and enable Sound Effects only for the deferred ambience experiment. Remaining gates are physical-iPad continuity/audio acceptance and Jeffrey's commit/push decision. Untracked `tmp/` remains protected and unrelated.

Simulated-exam continuity acceptance checkpoint 41, 2026-08-08: Jeffrey physically reviewed the published `e050e61` GitHub Pages build on the iPad and reported that the continuity release passed. This accepts the Full Mock narrative ordering, start-engine and join-traffic recordings/surfaces, unscored transitions, cleaned four-exit roundabout, persistence/resume behavior, complete five-examiner audio corpus, and iPad presentation as the current baseline. The release remains reproducible from the checkpoint-40 gate: 585/585 tests, package hash `346e48cf1eb0f5c75549134276e5fba5c1b02e2a9b2d780ee2f225e7578e0c3a`, 1,244 assets / 46,249,794 bytes, and 1,185/1,185 recorded variants. Solo E3 now begins as a separate reversible slice: correct post-answer movement for representative junction, roundabout, and parking/pull-over families, with no scoring changes, no crash depiction, and first-class reduced-motion behavior. Exact next action: complete the delegated design, route/geometry inventory, and sound/accessibility audit; then implement the smallest test-gated post-answer movement contract before expanding to other road families. `tmp/` remains protected and unrelated.
## Checkpoint 42 — Solo E3 post-answer movement baseline (2026-08-08)

- Jeffrey authorized autonomous work after physical-iPad acceptance of commit `e050e61`.
- Approved the bounded correct-only movement design and plan in:
  - `docs/superpowers/specs/2026-08-08-solo-e3-correct-post-answer-movement-design.md`
  - `docs/superpowers/plans/2026-08-08-solo-e3-correct-post-answer-movement.md`
- Baseline gate: `npm test` passes 585/585 before production integration.
- First slice remains visual-only: junction, four/five-exit roundabout, parking,
  and voluntary stopping. Incorrect, timeout, deferred Mock, Road movement Off,
  and reduced-motion reveals remain static. Continue never waits.
- Existing static correct-route geometry remains the source of truth; no scoring,
  persistence schema, audio, catalog, credential, or asset changes are authorized.
- Delegated pure contract and route-fixture audit use disjoint file fences. If an
  agent is interrupted, resume the existing files and handoff rather than restart.

## Checkpoint 43 — Retained route geometry green (2026-08-08)

- `junction-v2` now retains immutable `geometry.correctRoute` points from the
  photographed learner entry to the exact accepted road mouth.
- Four- and five-exit `roundabout-v2` models retain sampled lane points derived
  from their exact generated `routeCircle`, accepted `exitJoin`, and target.
- Static reveal rendering consumes those retained points; parking and stopping
  continue to use their existing reviewed template routes.
- Focused spatial, manoeuvre, and independent route-fixture audit gate passes
  40/40. No renderer animation, app lifecycle, scoring, persistence, audio,
  catalog, or asset behavior changed at this checkpoint.
- Mechanical rollback: revert the `correctRoute` additions/refactor in
  `src/spatial-surfaces.js`, the focused expectations in
  `tests/spatial-surfaces.test.js`, and remove the new fixture audit/handoff.

## Checkpoint 44 — Solo E3 automated and browser review gate (2026-08-08)

- Correct immediate Practice/Learn reveals now animate one decorative marker
  along the exact retained accepted route for junctions, four/five-exit
  roundabouts, parking, and voluntary stopping. The animation starts only
  after the attempt and active session are saved; it never records, scores,
  persists, or delays Continue.
- Incorrect, timeout, audio-failure, deferred Mock, unsupported-family, Road
  movement Off, and reduced-motion cases retain the prior static reveal.
- Full automated gate passes 612/612. `npm run release:check` builds a complete
  1,246-asset / 46,262,551-byte runtime with package hash
  `62a964c672c28137ba7221755c52265c8d812546013173fba8d7b7e7cfd3a7f9` and
  clean `git diff --check`.
- Real-browser review at iPad-landscape proportions confirmed a four-exit
  roundabout marker traverses the accepted lane, settles at the exact exit,
  and exposes Continue immediately. A deliberately incorrect response and a
  correct response with Road movement Off rendered no motion layer.
- No catalog, command/phrasing/action/surface/target ID, interface copy, audio,
  credential, dependency, network request, or persistence-schema change is in
  this slice. The prior static reveal remains the mechanical rollback path.
- Exact next gate: Jeffrey physically checks junction, both roundabout layouts,
  parking, pull-over, immediate Continue, Road movement Off, Reduce Motion,
  Mock withholding, and installed offline use on iPad. Do not perform release
  bookkeeping, commit, or push before that acceptance.

## Checkpoint 45 — Solo E3 physical-iPad acceptance (2026-08-08)

- Jeffrey reported that the physical iPad review passed, accepting supported
  correct-answer movement, immediate Continue, static fallback cases, Mock
  withholding, and installed offline behavior.
- README, active design, changelog, implementation spec, and plan now describe
  this as the first bounded Solo E3 slice rather than full driving simulation.
- The rollback boundary remains the two pure motion modules, retained route
  exposure, renderer options, delimited CSS, controller hook, and focused tests;
  removing them restores the accepted static `e050e61` reveal behavior without
  any catalog, attempt, storage, audio, or asset migration.
- Exact next action: rerun the complete release and credential audit, record its
  reproducible package result, and stop for Jeffrey's commit/push decision.

## Checkpoint 46 — Solo E3 release candidate complete (2026-08-08)

- Final `npm run release:check` passes 612/612 tests, a clean diff check, the
  repository/runtime credential-shaped-text audit, and the bilingual AI-voice
  disclosure audit.
- The deterministic offline package remains reproducible at 1,246 assets,
  46,262,551 bytes, package hash
  `62a964c672c28137ba7221755c52265c8d812546013173fba8d7b7e7cfd3a7f9`, and
  `Recorded corpus complete: true` for all 1,185 Spanish recordings.
- Automated, desktop-browser, and physical-iPad gates are complete. No required
  implementation or review work remains in this bounded Solo E3 slice.
- The working tree remains uncommitted. Exact next action is Jeffrey's explicit
  commit/push decision; do not mix later Solo E3 ideas into this rollback-safe
  release candidate.
