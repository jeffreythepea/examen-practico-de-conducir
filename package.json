{
  "name": "examen-practico-de-conducir",
  "version": "0.1.0",
  "description": "Standalone bilingual daily practice for Spanish practical-driving-exam commands",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "node scripts/serve.mjs",
    "serve:lan": "node scripts/serve.mjs --host 0.0.0.0",
    "release:check": "npm test && git diff --check",
    "optimize:images": "node scripts/optimize-runtime-images.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
